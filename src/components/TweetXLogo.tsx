import { Typography } from "@mui/material";

type Props = {};

export default function TweetXLogo({}: Props) {
  return (
    <Typography color="primary" variant="h5">
      TweetX
    </Typography>
  );
}
